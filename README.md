# terraform-null-child

Minimal Terraform child module for testing HCP Terraform Private Registry module
publishing and Terraform 1.15 dynamic module source expressions.

The module uses the `null` provider and exposes a `name` variable and output.
Publish version `1.0.0` from the repository root.
